import React from "react";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <main className="relative">
      {/* Hero Section */}
      <Hero />
      {/* Cards Section */}
      <section>Cards</section>
    </main>
  );
};

export default Home;
